# 🚀 Система аутентификации и авторизации на FastAPI 

## 📦 Стек технологий
- **FastAPI** — backend-фреймворк  
- **PostgreSQL** — база данных  
- **SQLAlchemy / Alembic** — ORM и миграции  
- **Docker / Docker Compose** — контейнеризация  
- **Pydantic** — валидация данных  

---

## ⚙️ Установка и запуск

### 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/m0r-h2/test_project.git
cd test_project
```

### 2️⃣ Сборка и запуск контейнеров
```bash
docker compose up -d --build
```

### 3️⃣ Применение миграций
```bash
docker compose exec web alembic upgrade head
```

### 4️⃣ Добавление тестовых пользователей
> ⚠️ Обратите внимание: тестовые данные добавляются **только если база данных пуста**.
```bash
docker compose exec web python -m app.seed
```

При успешном выполнении вы увидите сообщение:
```
Seed completed successfully!
```
### 5️⃣ Перейдите в документацию Swagger UI:
---
http://127.0.0.1:8000/docs#/
## 🧩 Функциональность приложения

Приложение реализует **систему аутентификации и авторизации** на основе **Access** и **Refresh токенов**.

### 👥 Возможности системы:
- Регистрация нового пользователя (с подтверждением пароля)  
- Авторизация с помощью JWT токенов  
- Обновление информации о пользователе  
- Мягкое (логическое) удаление аккаунта  
- Авторизация через **OAuth2**

---

## 🔐 Роли пользователей

### 👤 Пользователь (User)
- Создание собственного профиля  
- Редактирование информации своего профиля  
- Мягкое удаление (деактивация) своего аккаунта  

### 🛠️ Администратор (Admin)
- Все возможности роли **User**  
- Просмотр **активных** и **удалённых** пользователей  
- Изменение ФИО пользователя по его `id`  
- Мягкое удаление аккаунта по `id`

---

## 🧱 Модель пользователя (User)

- Пароли хранятся **в зашифрованном виде (hashed)**  
- Поддерживается **статус активности** (для мягкого удаления)  
- Пример структуры таблицы:

<img width="482" height="489" alt="image" src="https://github.com/user-attachments/assets/0923b0e7-df09-485a-ba82-a942dd8272d5" />

---

## 💡 Дополнительно

- Проект готов к расширению (можно добавить сервисы для логирования, метрик, тестов и т.п.)  
- При необходимости можно запустить `app.seed` автоматически при старте контейнера  
- Архитектура приложения модульная и легко масштабируется

---

## 🧑‍💻 Автор
**Система аутентификации и авторизации на FastAPI**  
Разработчик: [m0r-h2](https://github.com/m0r-h2)

